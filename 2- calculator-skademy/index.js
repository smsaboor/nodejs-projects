// index.js

// Function to add two numbers
function add(x, y) {
  return x + y;
}

// Function to add two numbers
function sub(x, y) {
  return x - y;
}

// Function to add two numbers
function mul(x, y) {
  return x * y;
}

// Function to add two numbers
function div(x, y) {
  return x / y;
}

// Export the add function
module.exports = { add, sub, mul, div };
